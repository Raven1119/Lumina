# Execution V2 source audit

Audited 2026-08-26. Current below means the cited official repository commit, pinned by full SHA. The first mapping covers append-only execution facts, derived state, bounded model context, and exact decision-request fidelity. The durable extension covers persistent root identity, durable EventLog/checkpoint recovery, and WAIT/event/resume. It does not authorize importing any source's larger runtime architecture.

## OpenAI Codex

- **SOURCE / COMMIT / VERSION / LICENSE:** [openai/codex at `a9ed4f154a4fad64acf538d6418d3ed012aeab86`](https://github.com/openai/codex/tree/a9ed4f154a4fad64acf538d6418d3ed012aeab86), rolling `main` (`codex-rs` workspace version `0.0.0` at this commit); [Apache-2.0](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/LICENSE). The full commit, not the placeholder workspace version, is the audited version.
- **SOURCE SYMBOL / PATH:** [`Session` / `SessionConfiguration`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/core/src/session/session.rs), [`TurnContext` / `make_turn_context`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/core/src/session/turn_context.rs), [`StepContext`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/core/src/session/step_context.rs), [`run_turn` / `run_sampling_request`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/core/src/session/turn.rs), [`ToolRouter::model_visible_specs`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/core/src/tools/router.rs), [`Prompt`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/core/src/client_common.rs), and [`ExecCommandToolOutput::response_text` / `truncated_output`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/core/src/tools/context.rs).
- **ORIGINAL INPUT / OUTPUT OR CONTRACT:** `TurnContext` holds turn-scoped configuration. `StepContext` is explicitly request-scoped and freezes the concrete model, environment, approvals, capability roots, MCP bindings, loaded instructions, and the finalized `ToolRouter` for one sampling request. `run_turn` captures that context once so prompt construction, advertised tools, and subsequent tool dispatch share the same request view. `Prompt` is the single-model-turn payload. Tool execution retains raw output while `response_text()` creates a bounded model-visible form with an explicit truncation warning and original token count.
- **BEHAVIOR BORROWED:** Capture one immutable request view before sampling; derive both the advertised tools and the provider payload from it; keep canonical tool facts distinct from their bounded model-visible projection.
- **LUMINA ADAPTATION:** Build the bounded `Context` and available-tool list first, pass that exact request object to `Model.decide`, then create the immutable `DecisionFrame` from the same request plus the returned response/action. A truncation marker is part of the request text whenever canonical output is shortened.
- **BEHAVIOR NOT COPIED:** Codex's session/task machinery, approvals, MCP/capability discovery, provider stack, async task lifecycle, and tool router. Codex does not supply a durable Lumina `DecisionFrame` contract; exact `actual_request` persistence is a Lumina boundary, not a claimed Codex feature.
- **DURABLE SOURCE SYMBOL / PATH:** [`thread-store` durable-handle contract](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/thread-store/src/lib.rs), [`CreateThreadParams` / `ResumeThreadParams` / `StoredThreadHistory` / `StoredModelContext`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/thread-store/src/types.rs), [`LiveThread::resume` / `append_items` / `flush`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/thread-store/src/live_thread.rs), [`ThreadManager::resume_thread_with_history`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/core/src/thread_manager.rs), [`RolloutRecorder::persist` / `flush`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/rollout/src/recorder.rs), and the [`thread/resume`, `turn/start`, `turn/interrupt` lifecycle](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/app-server/README.md).
- **DURABLE ORIGINAL BEHAVIOR (DIRECT FACT):** Codex says application code should treat `ThreadId` as its only durable thread handle. `CreateThreadParams` receives a thread id before opening persistence; `ResumeThreadParams` reopens that existing id for future appends. Stored history/model context remains in replay order. A runtime `Session` has at most one active task and can be interrupted; resuming uses the persisted conversation id and `LiveThread::resume`. `RolloutRecorder::flush` waits for queued writes, while `persist` materializes buffered rollout data and is idempotent.
- **DURABLE BEHAVIOR BORROWED / LUMINA ADAPTATION:** Separate long-lived identity from process, turn, and request lifetimes. Lumina persists `execution_id` and its one `root_actor_id`; a fresh Runtime reopens those ids and reconstructs state from durable facts before any new model call. `Checkpoint` accelerates that reconstruction but does not replace the EventLog.
- **DURABLE BEHAVIOR DELIBERATELY NOT COPIED:** Codex thread trees, `SessionId` ancestry, forks/subagents, rollout schema, app-server protocol, archive/delete/rollback, active-writer ownership, approval persistence, and its complete resume framework. Mapping Codex `ThreadId` to Lumina `execution_id` is an adaptation, not identity or format compatibility.

## DeepSeek Harness (DSH)

- **SOURCE / COMMIT / VERSION / LICENSE:** [deepseek-ai/deepseek-harness at `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`](https://github.com/deepseek-ai/deepseek-harness/tree/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e), `dsh 0.1.1-rc.2`; [MIT](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/LICENSE).
- **SOURCE SYMBOL / PATH:** [Session subsystem contract](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/docs/subsystems/session.md), [`SessionEventMap` / `SessionEvent` / `sourceEventSeqs`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/core/session/src/types.ts), [`Session.append` / `deriveMessages` / `deriveEventMessage`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/core/session/src/index.ts), [`foldSurface`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/core/session/src/surface.ts), and [`SessionPersistence`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/session/session-persistence/src/index.ts).
- **ORIGINAL INPUT / OUTPUT OR CONTRACT:** A `Session` is an append-only typed `SessionEvent` log and the single source of truth; model messages are derived, not separately authoritative. `append` assigns the next monotonic `seq`, snapshots/freezes and validates the event, then commits it. `sourceEventSeqs` cites earlier causal events—for example, an assistant message cites its streamed chunks and a tool result cites its tool call. `deriveMessages()` folds the log into the current model-visible surface. Persistence stores the existing event type contiguously and losslessly; it does not introduce a parallel persisted fact model.
- **BEHAVIOR BORROWED:** Append immutable facts with a stable sequence; derive working/model state by a pure fold; use explicit source-event references when a derived fact needs causal provenance.
- **LUMINA ADAPTATION:** Use the smallest fixed `Event` vocabulary needed by this slice and derive `ExecutionState` from the ordered tuple on demand. Preserve event identity/provenance in state and decision records; never mutate history to represent progress.
- **BEHAVIOR NOT COPIED:** DSH's extensible session ontology, surface replacement/compaction operations, streaming chunk graph, plugin hooks, and full agent loop. The audited DSH package is pre-release, and its persistence format is separately marked format version `0`; neither becomes a Lumina compatibility promise.
- **DURABLE SOURCE SYMBOL / PATH:** [`SessionPersistence.append` / `prepare` / `load`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/session/session-persistence/src/index.ts), [`SessionWriteBehind.flush`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/session/session-persistence/src/write-behind.ts), [`session-checkpoint-policy`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/session/session-checkpoint-policy/src/index.ts), [`interruptedTurnClosers`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/core/session/src/repair.ts), and the [persistence/flush/recovery contract](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/docs/subsystems/persistence.md).
- **DURABLE ORIGINAL BEHAVIOR (DIRECT FACT):** The durable unit remains the existing contiguous `SessionEvent`; there is no second persisted-message authority. In-memory `Session.append` commits synchronously and persistence observes it asynchronously through write-behind batching. `SessionWriteBehind.flush` cancels the batching wait and drains to quiescence; the checkpoint policy flushes before model dispatch and top-level tool dispatch. Cold load reconstructs the event log and appends deterministic interruption closers for an open tail turn. Committed corruption rejects, while only an incomplete final physical record may be discarded. `sourceEventSeqs` continues to identify earlier causal events.
- **DURABLE BEHAVIOR BORROWED / LUMINA ADAPTATION:** Persist the canonical event envelope itself; derive model-visible history and recovered state from that durable sequence; expose an explicit durability barrier before the next decision/effect boundary. Lumina deliberately strengthens DSH's hot-path ordering: `EventLog.append` is not authoritative until its local append-and-flush succeeds, so no accepted in-memory event can outrun the durable log. Lumina's materialized `Checkpoint` is a replay optimization, not DSH's semantic flush checkpoint.
- **DURABLE BEHAVIOR DELIBERATELY NOT COPIED:** Write-behind batching, service/plugin seams, JSONL compression, SQLite, session preparation/publication, surface repair, and synthetic unknown-outcome tool results. `interruptedTurnClosers` explicitly addresses a side-effect uncertainty window; this Lumina slice permits restart only after a durably settled action or at WAIT and does not reconcile UNKNOWN effects.

## Prime Agent

- **SOURCE / COMMIT / VERSION / LICENSE:** [PrimeIntellect-ai/prime-agent at `514633727bf26d74f39f3119c2b0e31a5ceb2a9d`](https://github.com/PrimeIntellect-ai/prime-agent/tree/514633727bf26d74f39f3119c2b0e31a5ceb2a9d), `v0.8.1`; [MIT](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/LICENSE).
- **SOURCE SYMBOL / PATH:** [core agent contract](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/agent/README.md), [`Agent`](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/agent/src/agent.ts), [`runAgentLoop` / `runAgentLoopContinue`](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/agent/src/agent-loop.ts), and [`AgentContext` / `AgentState` / `AgentTool` / `StreamFn`](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/agent/src/types.ts).
- **ORIGINAL INPUT / OUTPUT OR CONTRACT:** The documented model path is `AgentMessage[] -> transformContext() -> AgentMessage[] -> convertToLlm() -> Message[] -> LLM`: working messages and provider-visible messages are distinct projections. `AgentTool.execute` is a host callback; the loop invokes it and returns the outcome as a tool-result message. The low-level loop accepts an `AgentContext` plus configuration/provider stream function, exposing request/response and before/after-tool boundaries to the host.
- **BEHAVIOR BORROWED:** Keep authoritative side effects in host callbacks, and make agent-visible working/control state an input projection rather than authority. The “host owns execution” characterization is an inference from the official loop and `AgentTool.execute` contracts, not an explicit append-only guarantee in Prime Agent.
- **LUMINA ADAPTATION:** `ToolHost` alone executes a selected tool; `RootAgentProcess` appends the resulting fact, and the model can propose only a decision. The exact provider request and returned response are captured together at the boundary in `DecisionFrame`, while `ExecutionState` remains derived from facts.
- **BEHAVIOR NOT COPIED:** Prime Agent's mutable `AgentState`, persistent IPython/RLM execution, daemon/session infrastructure, extensions, branching, provider abstraction stack, and general coding-agent loop. Prime Agent is not used as evidence for append-only history or durable decision fidelity.
- **DURABLE SOURCE SYMBOL / PATH:** [`AgentSession` and `sessionId`](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/coding-agent/src/core/agent-session.ts), [`AgentSessionRuntime.switchSession` / `teardownForReplacement` / `buildAndApplyReplacement`](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/coding-agent/src/core/agent-session-runtime.ts), [`SessionManager.create` / `open` / `newSession` / `flushNow` / `buildSessionContext`](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/coding-agent/src/core/session-manager.ts), and the [SDK lifecycle contract](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/coding-agent/docs/sdk.md).
- **DURABLE ORIGINAL BEHAVIOR (DIRECT FACT):** `AgentSession` owns one live agent's messages/model/event lifecycle and obtains `sessionId` from `SessionManager`. Session replacement lives one layer out in `AgentSessionRuntime`: `switchSession` opens a persisted manager, tears down the old session, creates a replacement with `reason: resume`, then rebinds it. `SessionManager` stores the id in the JSONL header and `buildSessionContext` reconstructs model-facing messages from the selected persisted branch. `flushNow` forces pre-model state to disk.
- **DURABLE BEHAVIOR BORROWED / LUMINA ADAPTATION:** A live agent object is replaceable; persistent identity and reconstructable history sit at the host boundary. Lumina applies that separation to one Root: Runtime and `RootAgentProcess` objects may disappear, while `execution_id`, `root_actor_id`, durable events, and checkpoint metadata survive and are loaded before a new process is allowed to decide.
- **DURABLE BEHAVIOR DELIBERATELY NOT COPIED:** Prime's `AgentSessionRuntime`, session replacement leases, tree/branch JSONL schema, session migration, compaction, daemon/worker continuity, extensions, and IPython/RLM. **Inference:** Prime's placement of replacement in `AgentSessionRuntime` supports a host-lifecycle reading, but Prime does not state the Lumina rule that working `AgentState` can never be persistence authority.

## LongHorizon-Harness

- **SOURCE / COMMIT / VERSION / LICENSE:** [AMAP-ML/LongHorizon-Harness at `a1dd930614972b92361c1b9cd6aac441a6db5a65`](https://github.com/AMAP-ML/LongHorizon-Harness/tree/a1dd930614972b92361c1b9cd6aac441a6db5a65), `v0.1.7`; [MIT](https://github.com/AMAP-ML/LongHorizon-Harness/blob/a1dd930614972b92361c1b9cd6aac441a6db5a65/LICENSE).
- **SOURCE SYMBOL / PATH:** [run-record and role contract](https://github.com/AMAP-ML/LongHorizon-Harness/blob/a1dd930614972b92361c1b9cd6aac441a6db5a65/README.md), [`EpisodeResult` / `AuditReport` / `ManagedRound`](https://github.com/AMAP-ML/LongHorizon-Harness/blob/a1dd930614972b92361c1b9cd6aac441a6db5a65/src/lh_harness/types.py), [`run_role_manager` / `_record_round` / `_append_event`](https://github.com/AMAP-ML/LongHorizon-Harness/blob/a1dd930614972b92361c1b9cd6aac441a6db5a65/src/lh_harness/manager.py), and [`audit_report_from_episode_result` / `parse_audit_report`](https://github.com/AMAP-ML/LongHorizon-Harness/blob/a1dd930614972b92361c1b9cd6aac441a6db5a65/src/lh_harness/auditor_agent.py).
- **ORIGINAL INPUT / OUTPUT OR CONTRACT:** Each isolated run retains an event stream, task state, role trajectories, workspace, audits, and final report. `EpisodeResult` records an execution episode; `AuditReport` is a structured verification result; `ManagedRound` combines plan, executor output, audit, feedback, and derived task state. Resume reconstructs manager input from recorded rounds and verified task state rather than replaying raw prior prompts. A task is complete only when the latest independent audit is cleanly complete; rejected work remains evidence rather than progress.
- **BEHAVIOR BORROWED:** Preserve the distinction between execution evidence, derived progress state, and verification conclusions. Recovery and verification are useful evidence that these layers must not be conflated.
- **LUMINA ADAPTATION:** For this first slice, raw `EventLog` facts remain authoritative and `ExecutionState` is only their deterministic projection. Any result/verification fact must be appended before it can affect the fold.
- **BEHAVIOR NOT COPIED (FIRST SLICE):** Manager/Executor/Auditor roles, multi-round orchestration, dashboards, role prompts, external-process control, and independent verifier implementation. The durable extension below borrows recovery semantics only, not that architecture.
- **DURABLE SOURCE SYMBOL / PATH:** [`run_role_manager`, `_recorded_rounds`, `_record_round`, `_append_event`](https://github.com/AMAP-ML/LongHorizon-Harness/blob/a1dd930614972b92361c1b9cd6aac441a6db5a65/src/lh_harness/manager.py), [`EpisodeResult` / `AuditReport` / `ManagedRound`](https://github.com/AMAP-ML/LongHorizon-Harness/blob/a1dd930614972b92361c1b9cd6aac441a6db5a65/src/lh_harness/types.py), [`RunSupervisor.resume` / `_resume_once`](https://github.com/AMAP-ML/LongHorizon-Harness/blob/a1dd930614972b92361c1b9cd6aac441a6db5a65/src/lh_harness/supervisor/service.py), and [`LocalEnvironment._communicate_streaming`](https://github.com/AMAP-ML/LongHorizon-Harness/blob/a1dd930614972b92361c1b9cd6aac441a6db5a65/src/lh_harness/environment/local.py).
- **DURABLE ORIGINAL BEHAVIOR (DIRECT FACT):** On resume, a new worker reads completed `ManagedRound` records from append-only `rounds.jsonl`, restores the last plan/task state/contract, and rebuilds the next Manager prompt from task plus recorded rounds rather than restoring the prior model control flow. `RunSupervisor` reuses the run directory, logs, and control bus and increments a resume generation. Partial executor output remains available after timeout/cancellation. `_recorded_rounds` lets the latest duplicate round index win and skips unreadable/malformed lines, including a partial tail.
- **DURABLE BEHAVIOR BORROWED / LUMINA ADAPTATION:** Resume from durable facts and present external/workspace reality, not a serialized model stack or replayed old decision. Lumina's checkpoint records an `ExecutionState` snapshot plus `last_applied_event_sequence`; recovery verifies and folds the durable tail, while missing/stale checkpoints fall back to full EventLog replay.
- **DURABLE BEHAVIOR DELIBERATELY NOT COPIED:** Manager/Executor/Auditor ontology, run supervisor, process spawning, control bus, resume generations, multi-round ledgers, dashboards, and verification gates. No generic serialized execution-checkpoint source symbol was found in the audited recovery path; Lumina's `Checkpoint` format is therefore a local minimal adaptation. LongHorizon's malformed-line skipping is also rejected: malformed Lumina EventLog or explicitly corrupt checkpoint fails rather than being guessed around.

## Temporal

- **SOURCE / COMMIT / VERSION / LICENSE:** Temporal Server [`v1.31.2` at `19a774302c613da9adc4436ab14278ccdca8e0a5`](https://github.com/temporalio/temporal/tree/19a774302c613da9adc4436ab14278ccdca8e0a5), [MIT](https://github.com/temporalio/temporal/blob/19a774302c613da9adc4436ab14278ccdca8e0a5/LICENSE); Go SDK [`v1.48.0` at `b7c242c6894df088a57a85b33d0586e908da8b93`](https://github.com/temporalio/sdk-go/tree/b7c242c6894df088a57a85b33d0586e908da8b93), [MIT](https://github.com/temporalio/sdk-go/blob/b7c242c6894df088a57a85b33d0586e908da8b93/LICENSE); documentation at [`6f46de944c41b1823331536a65356548b94578c7`](https://github.com/temporalio/documentation/tree/6f46de944c41b1823331536a65356548b94578c7), [MIT](https://github.com/temporalio/documentation/blob/6f46de944c41b1823331536a65356548b94578c7/LICENSE.md). Server `main` was separately observed at `30e0884ac084ecb923b717c3f3f446f72d615e4d`; behavior below is pinned to the stable release sources.
- **SOURCE SYMBOL / PATH:** [`signalworkflow.Invoke`](https://github.com/temporalio/temporal/blob/19a774302c613da9adc4436ab14278ccdca8e0a5/service/history/api/signalworkflow/api.go), [`api.UpdateWorkflowWithNew`](https://github.com/temporalio/temporal/blob/19a774302c613da9adc4436ab14278ccdca8e0a5/service/history/api/update_workflow_util.go), [`respondactivitytaskcompleted.Invoke`](https://github.com/temporalio/temporal/blob/19a774302c613da9adc4436ab14278ccdca8e0a5/service/history/api/respondactivitytaskcompleted/api.go), Go SDK [`workflowTaskHandlerImpl.ProcessWorkflowTask` / `workflowExecutionContextImpl.ProcessWorkflowTask` / `matchReplayWithHistory`](https://github.com/temporalio/sdk-go/blob/b7c242c6894df088a57a85b33d0586e908da8b93/internal/internal_task_handlers.go), [Event History](https://github.com/temporalio/documentation/blob/6f46de944c41b1823331536a65356548b94578c7/docs/encyclopedia/event-history/event-history.mdx), and [Activity execution semantics](https://github.com/temporalio/documentation/blob/6f46de944c41b1823331536a65356548b94578c7/docs/encyclopedia/activities/activity-definition.mdx).
- **ORIGINAL BEHAVIOR (DIRECT FACT):** Temporal Event History is an append-only durable log used to reconstruct Workflow state after Worker failure. SDK replay regenerates Commands and checks them against recorded history; mismatches are nondeterminism. A Signal appends `WorkflowExecutionSignaled` and requests/schedules a Workflow Task when needed. Activity completion appends an `ActivityTaskCompleted` event and can schedule the next Workflow Task. A completion event prevents that Activity from being re-executed by Workflow replay.
- **BEHAVIOR BORROWED / LUMINA ADAPTATION:** Persist a wait before becoming quiescent; no resident model loop or polling is needed while waiting. `deliver_event` first appends the external event durably, then mechanically folds the typed wait condition; only a match makes Root runnable and permits a new bounded `Model.decide`. Restart reconstructs current state, not a prior Python/model stack. An Action with a durable result is already part of state and is not replayed.
- **BEHAVIOR DELIBERATELY NOT COPIED:** Temporal Server/Worker infrastructure, task queues, workflow-code replay, command nondeterminism machinery, Signals API, Activities, retries, heartbeats, timers, and distributed persistence. **Inference:** saying a Signal wakes a Workflow abbreviates durable history append plus a pending/new Workflow Task; it does not mean a continuously resident thread. Temporal also does not make an external Activity side effect atomic with its completion event: a crash between them can retry the Activity, so its idempotency advice confirms that Lumina's UNKNOWN side-effect window remains explicitly out of scope.

## Slice 4 delta: UNKNOWN Write reconciliation

This delta was re-audited before Slice 4 implementation. It narrows recovery to one observed failure: a durably started `WriteRequest` whose filesystem mutation may have completed before its result event was lost. It does not claim exactly-once execution and does not authorize a generic recovery engine.

### OpenAI Codex — action lifecycle and Host authority

- **SOURCE / COMMIT:** [openai/codex at `a9ed4f154a4fad64acf538d6418d3ed012aeab86`](https://github.com/openai/codex/tree/a9ed4f154a4fad64acf538d6418d3ed012aeab86), same Apache-2.0 pin audited above.
- **SOURCE SYMBOL:** [`stream_events_utils::handle_output_item_done`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/core/src/stream_events_utils.rs), [`ToolCallRuntime::handle_tool_call` / `handle_tool_call_with_source`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/core/src/tools/parallel.rs), [`ToolRouter::dispatch_tool_call_with_code_mode_result_inner`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/core/src/tools/router.rs), and [`ExecutedToolCallRecorder::record_tool_call` / `attach_pending_to_prompt`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/core/src/tools/executed_tool_calls.rs).
- **BORROWED SEMANTICS:** Codex first persists the completed model tool-call item, retaining its concrete `call_id`, name, and arguments, and only then queues Host-side dispatch. Execution is performed by the registered tool runtime and produces a separate response item. The optional `ExecutedToolCallRecorder` describes itself as best-effort, session-scoped attempted-tool metadata and explicitly notes that cancellation/compaction can leave pending calls unreported. Therefore a live session's attempted-call metadata is useful context but is not durable proof of either an external effect or its absence.
- **LUMINA ADAPTATION:** A durable `TOOL_CALL_STARTED` already carries the exact `WriteRequest` and its event identity before `ToolHost` writes. After process loss, the replacement Runtime treats the filesystem owned by the Host as the observation surface. Only exact equality between the requested logical content and the current target file may justify appending a new causally linked `ACTION_RECONCILED(confirmed_applied)` event. The old call event is not rewritten, and the Write is not dispatched again.
- **NOT COPIED:** Codex's async tool futures, parallel dispatch gates, cancellation protocol, session-scoped executed-tool metadata, provider response-item formats, approvals, and general tool router. The audited Codex source does not provide a generic durable post-crash reconciliation algorithm or prove exactly-once effects; Slice 4 does not claim otherwise.

### DeepSeek Harness — canonical UNKNOWN history repair

- **SOURCE / COMMIT:** [deepseek-ai/deepseek-harness at `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`](https://github.com/deepseek-ai/deepseek-harness/tree/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e), `dsh 0.1.1-rc.2`, MIT.
- **SOURCE SYMBOL:** [`TOOL_NOT_STARTED` / `TOOL_OUTCOME_UNKNOWN` / `interruptedTurnClosers`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/core/session/src/repair.ts), plus [`SessionEvent.sourceEventSeqs`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/core/session/src/types.ts).
- **BORROWED SEMANTICS:** DSH distinguishes a call that was never recorded as started from a recorded call whose completed outcome was not durably recorded. The latter is canonically `TOOL_OUTCOME_UNKNOWN`, and the synthetic closer cites the original `tool/call` sequence. Its model-visible repair text says not to retry a side-effecting operation blindly: first verify external state or ask the user. The append-only session log remains canonical; interrupted history is closed by later causal events rather than mutation of the original call.
- **LUMINA ADAPTATION:** Preserve the dangling `TOOL_CALL_STARTED` as the historical fact and append `ACTION_RECONCILED` only when exact filesystem evidence proves this one Write's intended state. The reconciliation event cites the original started-call event and records deterministic intended/observed evidence. If the target is missing, unreadable, or different, no success event is fabricated and recovery reports unresolved. A dangling `ShellRequest` remains explicitly unsupported.
- **NOT COPIED:** DSH's generic synthetic `tool/result`, provider-transcript closure, model-directed retry choice, step/turn closer generation, write-behind persistence, or extensible repair ontology. Lumina does not convert every UNKNOWN into an error result and does not let the model guess whether an effect occurred.

### Temporal — durable history is not exactly-once external state

- **SOURCE / COMMIT:** Temporal documentation at [`6f46de944c41b1823331536a65356548b94578c7`](https://github.com/temporalio/documentation/tree/6f46de944c41b1823331536a65356548b94578c7), MIT; server and Go SDK pins remain those audited above.
- **SOURCE SYMBOL:** [Activity Definition: `Idempotency`](https://github.com/temporalio/documentation/blob/6f46de944c41b1823331536a65356548b94578c7/docs/encyclopedia/activities/activity-definition.mdx), [Event History](https://github.com/temporalio/documentation/blob/6f46de944c41b1823331536a65356548b94578c7/docs/encyclopedia/event-history/event-history.mdx), and server [`respondactivitytaskcompleted.Invoke`](https://github.com/temporalio/temporal/blob/19a774302c613da9adc4436ab14278ccdca8e0a5/service/history/api/respondactivitytaskcompleted/api.go).
- **BORROWED SEMANTICS:** Workflow state is rebuilt from durable Event History, and a completed Activity does not re-execute during Workflow replay. But an Activity result is not recorded until it is reported; Temporal's official edge case says that if the function succeeds and the Worker crashes before notifying the Service, history lacks completion and the Activity is retried. Activity idempotency is therefore an application correctness requirement, not a platform guarantee that an arbitrary external effect occurs exactly once.
- **LUMINA ADAPTATION:** EventLog replay still determines Root state, but absence of `TOOL_RESULT` proves only that the result is unknown. Lumina does not retry this Write. It checks one deterministic postcondition already present in the request—current text exactly equals intended text—and records confirmation as a later history event. Any non-exact or unavailable observation stays UNKNOWN.
- **NOT COPIED:** Temporal Activities, retry policies, task queues, idempotency keys, heartbeats, distributed history service, Workflow code replay, or an “exactly once” claim. Slice 4 adds neither automatic retry nor a transaction/idempotency framework.

### LongHorizon-Harness — current reality outranks old control-flow claims

- **SOURCE / COMMIT:** [AMAP-ML/LongHorizon-Harness at `a1dd930614972b92361c1b9cd6aac441a6db5a65`](https://github.com/AMAP-ML/LongHorizon-Harness/tree/a1dd930614972b92361c1b9cd6aac441a6db5a65), `v0.1.7`, MIT.
- **SOURCE SYMBOL:** [`run_role_manager` resume path / `_recorded_rounds`](https://github.com/AMAP-ML/LongHorizon-Harness/blob/a1dd930614972b92361c1b9cd6aac441a6db5a65/src/lh_harness/manager.py), [`build_role_executor_prompt` / `build_role_auditor_prompt`](https://github.com/AMAP-ML/LongHorizon-Harness/blob/a1dd930614972b92361c1b9cd6aac441a6db5a65/src/lh_harness/role_prompts.py), and [`LocalEnvironment._communicate_streaming`](https://github.com/AMAP-ML/LongHorizon-Harness/blob/a1dd930614972b92361c1b9cd6aac441a6db5a65/src/lh_harness/environment/local.py).
- **BORROWED SEMANTICS:** Resume rebuilds the next manager input from durable rounds/task state rather than restoring a prior model stack. The official auditor prompt declares the real workspace and current target application to be the independent evidence boundary: it must inspect claimed files/state directly, while executor text, trajectories, and prior reports are only run records and not standalone completion proof. Partial executor output may survive interruption, but it remains evidence rather than authority.
- **LUMINA ADAPTATION:** Recovery ignores vanished control flow and compares durable Write intent with the target file as it exists now. A current exact match is admissible evidence; an old “write succeeded” claim, a started event by itself, or a mismatching file is not. This is a deterministic single-file inspection, not a new Auditor role.
- **NOT COPIED:** Manager/Executor/Auditor agents, natural-language verification, run supervisor, dashboards, permissive malformed-tail recovery, or a general completion verifier. No byte/content-level crash reconciler was found in audited LongHorizon; exact equality is Lumina's task-specific adaptation.

### Prime Agent — live session is not external-state authority

- **SOURCE / COMMIT:** [PrimeIntellect-ai/prime-agent at `514633727bf26d74f39f3119c2b0e31a5ceb2a9d`](https://github.com/PrimeIntellect-ai/prime-agent/tree/514633727bf26d74f39f3119c2b0e31a5ceb2a9d), `v0.8.1`, MIT.
- **SOURCE SYMBOL:** [`AgentTool.execute` / `AgentState` / `AgentContext`](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/agent/src/types.ts), [`runAgentLoop` / `runAgentLoopContinue`](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/agent/src/agent-loop.ts), [`AgentSessionRuntime.switchSession` / `teardownForReplacement` / `buildAndApplyReplacement`](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/coding-agent/src/core/agent-session-runtime.ts), and [`SessionManager.open` / `buildSessionContext`](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/coding-agent/src/core/session-manager.ts).
- **BORROWED SEMANTICS:** Tool execution is a Host callback and a live `AgentSession` can be torn down and replaced from `SessionManager` state. This supports separation between replaceable working-session state and Host-owned external effects. **Inference, not an explicit Prime guarantee:** mutable working `AgentState` cannot by itself establish what a filesystem callback changed after process loss.
- **LUMINA ADAPTATION:** Replacement Runtime state comes only from durable Lumina events; filesystem truth comes from `ToolHost` inspection. Neither the old model response nor vanished process is success authority. Only the evidence-bearing reconciliation event can move the fold beyond UNKNOWN Write.
- **NOT COPIED:** Prime's mutable `AgentState`, IPython/RLM, daemon/session replacement, extensions, branches, providers, or any append-only/exactly-once guarantee. Prime is used only for Host/working-session ownership, with inference explicit.

## Slice 5 delta: deterministic completion verification

This delta was re-audited before Slice 5 implementation. The research question is deliberately narrower than general task review: a Root may claim completion, but only one caller-supplied, immutable `FileContentEquals(path, expected_content)` specification and current filesystem evidence may authorize completion. No natural-language goal interpretation, Reviewer Agent, generic verifier registry, or composite predicate is authorized.

### LongHorizon-Harness — direct claimed-versus-audited completion boundary

- **SOURCE / COMMIT / VERSION:** [AMAP-ML/LongHorizon-Harness at `a1dd930614972b92361c1b9cd6aac441a6db5a65`](https://github.com/AMAP-ML/LongHorizon-Harness/tree/a1dd930614972b92361c1b9cd6aac441a6db5a65), `v0.1.7`, MIT.
- **SOURCE SYMBOL:** [`run_role_manager` completion branch / `_latest_auditor_is_clean_complete` / `_final_report`](https://github.com/AMAP-ML/LongHorizon-Harness/blob/a1dd930614972b92361c1b9cd6aac441a6db5a65/src/lh_harness/manager.py), [`build_role_auditor_prompt`](https://github.com/AMAP-ML/LongHorizon-Harness/blob/a1dd930614972b92361c1b9cd6aac441a6db5a65/src/lh_harness/role_prompts.py), and [`AuditReport` / `ManagedRound`](https://github.com/AMAP-ML/LongHorizon-Harness/blob/a1dd930614972b92361c1b9cd6aac441a6db5a65/src/lh_harness/types.py).
- **DIRECT SOURCE FACT:** A Manager `Next: done` is not accepted by itself. `run_role_manager` sets completion only when `_latest_auditor_is_clean_complete` finds a report whose status is `complete`, integrity is `clean`, and contract audit is `aligned`; otherwise it records invalid-completion feedback and continues management. The auditor prompt makes the real workspace/current target application the independent evidence boundary and says Executor text is only a claim. `_final_report` explicitly makes final status a harness-level decision rather than the last executor's self-claim.
- **BORROWED SEMANTICS:** Completion authority must be separated from agent output, and the deciding evidence must come from the current environment rather than a completion sentence or run record.
- **LUMINA ADAPTATION:** Replace LongHorizon's open-ended LLM audit with exactly one mechanical predicate fixed by the caller at execution creation: compare the current target file's logical UTF-8 content with the immutable `FileContentEquals.expected_content`. A Root emits only `ClaimComplete`; Runtime appends the claim, observes the environment, then appends either verified completion or a bounded rejection observation that returns control to Root.
- **NOT COPIED:** Manager/Executor/Auditor roles, natural-language task contracts, LLM auditing, multi-round orchestration, integrity/contract taxonomies, dashboards, or final-response generation. **NO DIRECT SOURCE IMPLEMENTATION:** LongHorizon contains a direct claimed-versus-audited completion gate, but not a predeclared typed deterministic `FileContentEquals` verifier or Lumina's three-event causal chain.

### OpenAI Codex — authoritative tool outcomes are not task acceptance

- **SOURCE / COMMIT / VERSION:** [openai/codex at `a9ed4f154a4fad64acf538d6418d3ed012aeab86`](https://github.com/openai/codex/tree/a9ed4f154a4fad64acf538d6418d3ed012aeab86), rolling `main` pin (`codex-rs` workspace version `0.0.0`), Apache-2.0.
- **SOURCE SYMBOL:** [`run_turn` / `run_sampling_request`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/core/src/session/turn.rs), [`ToolCallRuntime::handle_tool_call`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/core/src/tools/parallel.rs), [`ToolRouter::dispatch_tool_call_with_code_mode_result_inner`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/core/src/tools/router.rs), [`ExecCommandToolOutput::response_text` / `truncated_output`](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/core/src/tools/context.rs), and the [`turn/start` / `turn/completed` lifecycle](https://github.com/openai/codex/blob/a9ed4f154a4fad64acf538d6418d3ed012aeab86/codex-rs/app-server/README.md).
- **DIRECT SOURCE FACT:** Host-side tool dispatch produces a separate tool response. For command/test execution, `ExecCommandToolOutput` retains raw output plus process/exit metadata and constructs a bounded model-visible response. The turn loop ends when sampling no longer requires follow-up; the app-server's `turn/completed` denotes the terminal state of that turn. A test command can therefore provide authoritative Host-produced evidence, but turn completion itself does not compare that evidence with a caller-declared task acceptance predicate.
- **BORROWED SEMANTICS:** Keep Host-observed environment/tool outcomes distinct from the model's final message and from the lifecycle fact that a turn stopped.
- **LUMINA ADAPTATION:** `ClaimComplete` is only a Root decision. Runtime reads the execution-start `CompletionSpec` and current Environment directly; only its deterministic match may produce `COMPLETION_VERIFIED` and then `EXECUTION_COMPLETED`. Rejection becomes bounded model-visible evidence on the next request rather than a Host-authored repair.
- **NOT COPIED:** Codex's complete turn/session framework, async tool router, command runtime, hooks, reviews, approvals, provider response items, and test-running workflow. **NO DIRECT SOURCE IMPLEMENTATION:** No general predeclared environment-backed completion verifier matching this Slice was found in the audited Codex paths; `turn/completed` is not evidence that arbitrary task acceptance conditions are true.

### DeepSeek Harness (DSH) — canonical result provenance, not verified task completion

- **SOURCE / COMMIT / VERSION:** [deepseek-ai/deepseek-harness at `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`](https://github.com/deepseek-ai/deepseek-harness/tree/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e), `dsh 0.1.1-rc.2`, MIT.
- **SOURCE SYMBOL:** [`SessionEventMap` / `TurnEndReasonMap` / `SessionEvent.sourceEventSeqs`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/core/session/src/types.ts), [`Session.append` / `deriveMessages`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/core/session/src/index.ts), [`deriveEventMessage`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/core/session/src/surface.ts), and [`appendToolCall` / `appendToolResult`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/core/agent-loop/src/tool-calls.ts).
- **DIRECT SOURCE FACT:** The append-only `SessionEvent` log is canonical and model history is derived by fold. A `tool/result` is a later event whose `sourceEventSeqs` cites its `tool/call`, preserving result causality without rewriting the call. DSH also has `turn/end` with reason `{kind: 'completed'}`, but that is a turn-lifecycle boundary; as a non-surface event it does not carry `sourceEventSeqs`, and its type does not express external task acceptance.
- **BORROWED SEMANTICS:** Record claims, observed results, and terminal facts as distinct append-only events; let derived state/model context follow those facts; keep causal references explicit where one fact justifies the next.
- **LUMINA ADAPTATION:** Append `COMPLETION_CLAIMED`, then a causally linked `COMPLETION_VERIFIED` or `COMPLETION_REJECTED`; only verified may causally produce `EXECUTION_COMPLETED`. Fold rejection into runnable state plus a structured bounded Observation, and fold verified/completed history into terminal state on restart.
- **NOT COPIED:** DSH's extensible event ontology, plugin ecosystem, session surface replacement, generic agent loop, turn/step protocol, and persistence backends. **NO DIRECT SOURCE IMPLEMENTATION:** DSH provides canonical event/result provenance but no claimed-completion → environment-verification → execution-completion mechanism; Lumina's completion causal chain is a task-specific adaptation.

### Prime Agent — agent-loop termination is not world truth

- **SOURCE / COMMIT / VERSION:** [PrimeIntellect-ai/prime-agent at `514633727bf26d74f39f3119c2b0e31a5ceb2a9d`](https://github.com/PrimeIntellect-ai/prime-agent/tree/514633727bf26d74f39f3119c2b0e31a5ceb2a9d), `v0.8.1`, MIT.
- **SOURCE SYMBOL:** [`AgentTool.execute` / `AgentContext` / `AgentState` / `AgentEvent`](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/agent/src/types.ts), [`runAgentLoop` / `runAgentLoopContinue` and Host tool dispatch](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/agent/src/agent-loop.ts), and [`Agent`](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/agent/src/agent.ts).
- **DIRECT SOURCE FACT:** Tool execution is performed through the Host-supplied `AgentTool.execute` callback and returned as a tool-result message. The agent loop's messages/events describe working interaction and loop termination. **Inference, not an explicit Prime guarantee:** because the external effect occurs behind the Host callback, an assistant/final message or mutable `AgentState` alone cannot establish the current filesystem state.
- **BORROWED SEMANTICS:** Agent output may propose that work is done, while Host-owned world observation remains the authority for external state.
- **LUMINA ADAPTATION:** Root cannot provide expected content, a custom verifier, or `success=True` in `ClaimComplete`; it can only request verification against the immutable execution-start spec. Runtime performs the one filesystem comparison outside `ToolHost` and returns rejection evidence to the same Root when reality disagrees.
- **NOT COPIED:** Prime's mutable `AgentState`, provider stack, daemon/session machinery, extensions, branching, `shouldStop` controls, persistent IPython, or RLM. **NO DIRECT SOURCE IMPLEMENTATION:** No caller-fixed typed environment completion predicate or durable causal completion verification was found in the audited Prime Agent core; the Host/world-authority reading is intentionally limited and marked as inference.

## Slice 6 delta: DeepSeek native model adapter

This delta was re-audited before Slice 6 implementation. It borrows only the native Chat Completions call/result protocol and the separation between provider translation, Host execution, and durable facts. It does not authorize a provider framework, streaming, reasoning mode, retry policy, multi-tool execution, or a private adapter transcript.

### DeepSeek official API - current wire authority

- **SOURCE / COMMIT / VERSION / LICENSE:** [DeepSeek Chat Completions](https://api-docs.deepseek.com/api/create-chat-completion/), [Tool Calls](https://api-docs.deepseek.com/guides/tool_calls), and [Thinking Mode](https://api-docs.deepseek.com/guides/thinking_mode/), live official documentation audited 2026-08-26. **COMMIT: N/A** because these are mutable documentation pages rather than a versioned source checkout. No documentation license is stated on the cited pages.
- **SOURCE SYMBOL:** `POST /chat/completions`; request fields `model`, `messages`, `tools`, `thinking`, and `stream`; response fields `choices[].message.tool_calls[].id`, `.function.name`, and `.function.arguments`; continuation message fields `role: tool` and `tool_call_id`.
- **BORROWED SEMANTICS:** Use the current exact model id `deepseek-v4-pro`; send native function schemas; preserve the provider-issued call id on the matching tool result; explicitly select non-thinking mode with `thinking: {type: disabled}` because the current schema defaults thinking to enabled; use one non-streaming request against the documented `https://api.deepseek.com` base.
- **LUMINA ADAPTATION:** `DeepSeekModel` converts one provider call into one existing typed `ToolCall`, `Wait`, or `ClaimComplete`. Runtime executes the existing Host action, persists the original call id with the decision and structured observation, and reconstructs the next assistant-tool-call plus native `role=tool` continuation from durable `DecisionFrame` facts and the current bounded context. The prior user projection and current tool-result projection share one aggregate dynamic-content budget, so native message structure cannot double the Runtime context limit; the fixed system text and five fixed schemas carry no execution data. The API key is resolved from `DEEPSEEK_API_KEY` only inside the HTTP send boundary and is absent from payload snapshots and events.
- **NOT COPIED:** Beta strict mode, Responses API, Anthropic compatibility, thinking/reasoning content, streaming/SSE, parallel or multiple tool calls, retries, fallbacks, SDK session state, and any generic OpenAI-compatible provider abstraction. **NO DIRECT SOURCE IMPLEMENTATION:** DeepSeek documents the wire protocol but not Lumina's append-only EventLog, restart reconstruction, bounded projection, or DecisionFrame fidelity contract.

### DeepSeek Harness (DSH) - provider-neutral translation and call identity

- **SOURCE / COMMIT / VERSION / LICENSE:** [deepseek-ai/deepseek-harness at `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`](https://github.com/deepseek-ai/deepseek-harness/tree/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e), `dsh 0.1.1-rc.2`, MIT.
- **SOURCE SYMBOL:** [`LlmAdapter` / `GenerateOptions`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/docs/user/develop/practice/llm-adapter.md), [`ToolCallBlock` / `ToolResultBlock`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/llm/llm/src/types.ts), [`createToolResultMessage`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/llm/llm/src/message.ts), and DeepSeek adapter request serialization in [`serialize.ts`](https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/llm/llm-deepseek/src/serialize.ts).
- **BORROWED SEMANTICS:** The adapter is a translation boundary between a provider-neutral request and provider wire values. `ToolCallBlock.id` and `ToolResultBlock.toolCallId` are the same typed correlation identity, while arguments remain raw JSON until the adapter validates/translates them.
- **LUMINA ADAPTATION:** Keep the Runtime action vocabulary unchanged and add only a small native decision envelope carrying the exact provider request, secret-safe response, typed action, provider call id, and explicit failure. Reject zero, unknown, malformed, or multiple calls atomically before Host execution.
- **NOT COPIED:** DSH's adapter registry, plugin/service system, streaming chunks, retry policy, provider catalog, prepared-call lifecycle, session surface, or agent loop. Lumina uses one fixed DeepSeek adapter and one synchronous response because those are the only current callers.

### OpenAI Codex - native call/output boundary and request evidence

- **SOURCE / COMMIT / VERSION / LICENSE:** [openai/codex at `f5420174dafba153913a3e697f89002c338dfd7e`](https://github.com/openai/codex/tree/f5420174dafba153913a3e697f89002c338dfd7e), rolling `main`, Apache-2.0.
- **SOURCE SYMBOL:** provider-native `ResponseItem::FunctionCall` / function-call output items in [`codex-rs/protocol/src/models.rs`](https://github.com/openai/codex/blob/f5420174dafba153913a3e697f89002c338dfd7e/codex-rs/protocol/src/models.rs), [`Prompt`](https://github.com/openai/codex/blob/f5420174dafba153913a3e697f89002c338dfd7e/codex-rs/core/src/client_common.rs), [`run_turn`](https://github.com/openai/codex/blob/f5420174dafba153913a3e697f89002c338dfd7e/codex-rs/core/src/session/turn.rs), and [`RolloutRecorder`](https://github.com/openai/codex/blob/f5420174dafba153913a3e697f89002c338dfd7e/codex-rs/rollout/src/recorder.rs).
- **BORROWED SEMANTICS:** Treat a provider function call and the Host-produced function output as distinct typed items correlated by the original call identity. Capture the actual request boundary and response used for a decision rather than reconstructing an idealized request later.
- **LUMINA ADAPTATION:** `DecisionFrame` retains both the actual bounded Lumina `ModelRequest` and the exact secret-free DeepSeek wire request/response, resulting typed action, call id, and source event references. Native continuation is rebuilt from that durable frame plus the current structured observation; no adapter-owned message list becomes a second history.
- **NOT COPIED:** Codex sessions, rollout format, Responses protocol, provider stack, approvals, parallel tool router, MCP, sandbox policy, compaction, or complete turn framework. Codex is a protocol-boundary reference, not a source for Lumina's runtime semantics.

### Prime Agent - Host-owned tool execution

- **SOURCE / COMMIT / VERSION / LICENSE:** [PrimeIntellect-ai/prime-agent at `514633727bf26d74f39f3119c2b0e31a5ceb2a9d`](https://github.com/PrimeIntellect-ai/prime-agent/tree/514633727bf26d74f39f3119c2b0e31a5ceb2a9d), `v0.8.1`, MIT.
- **SOURCE SYMBOL:** [`AgentTool.execute` / `AgentToolCall` / tool events](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/agent/src/types.ts), [`runAgentLoop` Host dispatch](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/agent/src/agent-loop.ts), and [`ToolResultMessage`](https://github.com/PrimeIntellect-ai/prime-agent/blob/514633727bf26d74f39f3119c2b0e31a5ceb2a9d/packages/ai/src/types.ts).
- **BORROWED SEMANTICS:** Provider output requests a call; the Host owns execution and emits a correlated tool-result message. Provider output is therefore not filesystem truth.
- **LUMINA ADAPTATION:** `DeepSeekModel` can only propose the existing typed action. `ToolHost` remains the sole effect authority, canonical `ToolResult` remains separate from its bounded provider-visible projection, and completion still requires Runtime verification against the caller-owned spec.
- **NOT COPIED:** Prime's provider stack, mutable agent state, parallel batching, extensions, session daemon, IPython/RLM, or agent loop. **NO DIRECT SOURCE IMPLEMENTATION:** Prime demonstrates Host dispatch and correlated results but not Lumina's durable call-id reconstruction or EventLog/State/Context separation.

## Slice-level conclusions

The first-slice mapping remains: DSH-style append-only facts and pure derivation; Codex-style request-scoped tool/context snapshot and explicit bounded projection; Prime-style host-controlled tool execution; and LongHorizon's separation of evidence from derived/verified state. `DecisionFrame.actual_request` remains Lumina-specific: it is the exact bounded request object supplied to `Model.decide`, never a later reconstruction.

The durable extension is deliberately smaller than every upstream: Codex contributes identity/lifecycle separation and the boundary between a persisted call and Host dispatch; DSH contributes canonical UNKNOWN repair with causal references and the instruction to verify external state before any retry; Prime contributes replaceable live-session semantics under a host; LongHorizon contributes recovery/audit from durable records plus current workspace reality instead of old model control flow; Temporal contributes durable event history and the warning that Activity retry is not exactly-once external execution. Lumina keeps one Root, a durable-first local EventLog, a non-authoritative checkpoint validated against replay, zero model sampling while WAITING, typed mechanical wake, no replay of durably completed actions, and exactly one Slice 4 rule: an interrupted Write may be confirmed only by exact current filesystem evidence and a later causal event.

Slice 5 borrows only LongHorizon's demonstrated separation between an agent's completion claim and an independently accepted result, plus Codex/Prime Host authority and DSH causal event truth. The implementation target is intentionally much smaller: one immutable caller-owned `FileContentEquals` spec, one Runtime-owned deterministic Environment read, and append-only claimed/verified-or-rejected/completed facts. Codex, DSH, and Prime have **NO DIRECT SOURCE IMPLEMENTATION** of that mechanism; LongHorizon has the authority split but uses an LLM Auditor rather than this deterministic predicate. Nothing here authorizes a Reviewer Agent, generic verifier framework, multiple predicates, or natural-language completion judgment.

Slice 6 borrows the exact DeepSeek Chat Completions native-tool wire contract, DSH's provider-neutral call/result identity, Codex's typed call/output and request-boundary semantics, and Prime's Host execution boundary. Lumina adapts them as one thin fixed-model adapter whose continuation is rebuilt from durable facts and bounded observations. It deliberately does not copy a provider registry, SDK transcript, streaming/retry machinery, multi-tool scheduling, thinking mode, IPython, or any source project's complete agent loop.

## Source ambiguities and non-equivalences

- **Direct source fact:** every positive upstream behavior above names a pinned symbol or official contract. **Inference** is marked explicitly for Prime's host-authority reading and Temporal's wake shorthand.
- Codex rolling `main` moved since the first audit; the new full SHA is the reproducibility pin. Its identifiers and rollout format are not Lumina formats.
- DSH commits to the in-memory log before asynchronous persistence and relies on explicit flush barriers. Lumina's task contract is stricter: persistence succeeds before an event becomes authoritative.
- LongHorizon recovery skips malformed ledger lines and has no audited generic serialized-checkpoint symbol. Lumina instead fails malformed history/corrupt checkpoints and treats its checkpoint as a local replay optimization.
- Temporal guarantees deterministic Workflow replay against durable history, not exactly-once arbitrary external side effects. This audit authorizes only the task-card's deterministic Write equality check; all ambiguous Writes and all other UNKNOWN effect kinds remain unresolved.
