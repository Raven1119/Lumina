# 认知与执行实验简史

2026-09-09 按 owner 指令将原始请求、轨迹、测量输出、逐轮报告和历史代码副本移出项目。
本表保留阶段结论；不把失败重写为成功，也不再宣称这些摘要能重放原始运行。
已有 Git 提交未改写。已批准清理的文件均保留项目外的持久恢复副本，包括此前回收的交付物与 Memory 实验目录。恢复副本不提交 Git。

| 阶段 | 结论与边界 |
| --- | --- |
| Execution V2 | EXECUTION_V2_READY_TO_FREEZE：执行基座成立。实际 IPython 小样本 PROMOTE、PRIME_LIKE_SURFACE_BETTER；Child NOT_USED、拓扑 NOT VALIDATED、Prime parity UNVERIFIABLE。 |
| E0–E4 | E0 PASS 仅确定性投递；E1–E4 INCONCLUSIVE，没有建立独立 Mind 行为优势。 |
| W0 / S0 | W0 PASS：确定性预测、观察、比较。S0 INCONCLUSIVE：零模型调用，终态历史不能重放原 PRE。 |
| W1–W4 | INCONCLUSIVE：真实 Builder 的语义、续接和工具调用基数问题仍存在。 |
| W5–W8 | W5/W6 PASS 仅限冻结协议和动力学样本；W7/W8 INCONCLUSIVE，未知情况仍有终态协议失败。 |
| D1–D3 | INCONCLUSIVE：零有效指导或协议失败。D1 的 9/12 与 7/12 不构成 Mind 收益证据。 |
| D4–D6 | D4 受控真实反馈与恢复成立，完整标准未过；D5/D6 部分修复，新的完整指导闭环未成立。比较资格 NOT_YET。 |
| D7–V22 | D7 PARTIAL_REPAIR_WITH_CONTROLLED_LOOP，独立验收未过。早期集成有不确定性错误；V20–V22 完成受界恢复和三个新样本，非普遍可靠性证明。 |
| V23–V39 | 当时 ZIP 交付未完成；指导保留与结构逐步修复，业务程序和模型仍有错误。 |
| V40–V58 | ZIP 主任务累计 381 次、变体 83 次后完成，投递、执行、反馈、静默恢复成立；变体需要开发者明确验收要求，不计作自主发现。修订模型吸收已有实测，不是独立未来预测验证。 |
| V59–V69 | 用户事件先 Mind、上下文、认知提交和协议修复；旧认知自主修订仍失败。V63 AUTONOMOUS_REVISION_NOT_SUPPORTED；V69 文件已正确，但状态错配和重复指导尚未闭合。 |
| V70–V73 | 保留历史的旧命题状态由 Mind 修正；三个跨事件种子样本和新 CLI 文件任务完成，反馈后修正新错误假设并正常收尾。28/30 次含 6 次无效验收调用及 1 次 ConnectError。Builder 仅验证精确计算；输入为 ASCII。开发修复与显式恢复仍参与，非通用自治或单变量因果收益证明。 |

The promoted mechanisms now have formal current modules. Pure campaign drivers,
fixtures, old evaluator tests and V1-V72 compatibility branches are retired.
There is no central Session/Host. Current invariant tests use independent
synthetic inputs; they do not replay archived provider responses. The refactor
adds no real-model result and does not change any verdict in the table above.

当前接口和使用方法见 [INTEGRATED_CHAIN](INTEGRATED_CHAIN.md)，
当前能力及限制见 [CURRENT_STATUS](../../docs/CURRENT_STATUS.md)。

## 2026-09-10：恢复与任务工作上下文

R 恢复补强、E 执行交接、M 认知背景已完成。确定性回归覆盖真实子进程退出、
已知结果续接、未知副作用停止、冷内核计划退役、只读状态及各 owner 至少三次压缩与重启。
最终维护套件 **821 通过、30 跳过**；此前同工作树的 Docker 检查 **31 通过**，
该 Docker 检查未覆盖最后的 v4 摘要协议，真实 CLI 使用了 v4。

固定前缀的 baseline／mask／summary 小样本及修复共 10 次真实调用；summary 未证明
普遍优势，因此 baseline 仍为默认。真实 CLI 用 38 次调用完成文件清单、独立分析、
外部实测、报告更新及同一 Mind 的反馈。所选 CSV 的 278 字节与该序列化计算一致；
未执行 JSON，未登记前瞻 watch，不构成自动预测监测或完整轨迹验证。
完成后无待处理事件，安静恢复新增调用为零。

整轮 **48/64 次真实调用**，预留输出 491520 tokens，实际请求体 1715622 bytes；
provider 分别报告 input 385080、output 70192、cache-read input 85760 tokens。
六次摘要拒收、四次计算失败及两次最终决策协议拒收均保留；传输错误为零。
修复了短历史无法压缩、合法来源未进入引用目录和已知摘要失败的显式续接。
v4 将摘要字符数改为起草目标，保留 provider 输出、完整请求及累计预算硬边界；
旧协议和失败结果未被改写。旧认知仍有把文本长度夸大为原始字节证据的表述，
即使后来的独立实测支持最终数值，也不能算作全部认知正确或通用可靠性证明。

详细报告、预注册、原始请求及任务产物仅在本地恢复存储保留；公开树保留当前代码、
必要回归、设计与操作契约和本节结论。
