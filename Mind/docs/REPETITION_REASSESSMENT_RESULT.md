# Repetition observation and bounded reassessment

2026-09-13. Base: `b7277c383e1607bc1b6197797f9bd45cb7d189c5`.
**INCONCLUSIVE; neither B nor C promoted. Default remains off.**
The original Stage1 PARTIAL verdict and all 96 calls are unchanged. Hash checks
confirmed all 230 original state files and five workspace files unchanged.

## Confirmed gap and implementation

Read-only archive inspection found calls 77–83 had identical committed Python,
complete results and source bindings. Call 84 changed the inspection, with the
same source binding. Current checkpoint placement had not ended the loop. The
later exit followed disclosed developer feedback; calls 85 and 94 were NoChange.
This establishes a missing automatic observation path, not proof that another
Mind Directive would have solved the original scene.

The opt-in implementation reuses Execution's committed EventLog and provider
bindings, immutable evidence, Nervous triggers/outbox, and the same persistent
Mind with its existing reads, cognitive commits and original advice delivery.
There is no new host, judgment agent, store, scheduler or semantic action gate.

`--repetition-mode off|execution|mind` is fixed at start. Three consecutive
identical single IPython cells with successful, complete, identical observed
results qualify only under the same Run/Task and unchanged sampled sources and
owner input. Retired, unresolved, truncated, failed and explicit request_mind
cells do not qualify. Ordinary repeated outputs do not establish no progress,
failure or task completion; Python memory and unobserved external state remain
outside this comparison. Alternating loops and equivalent rewritten code are
outside this first slice.

Execution projects a compact fact with count, decision range, source/record/sample
references, Run and review status. Original bodies remain with Execution and
are retrieved through existing bounded reads. Mode B projects it only to
Execution; mode C also sends the existing execution.changed event to Mind.
Mind may read, analyze, issue advice or NoChange. One unchanged segment publishes
once across polls and restart; neither NoChange nor advice rearms notification.
Source sampling changes and new Task/Run or owner input create a new boundary;
source identity cannot collapse a later A-B-A occurrence into an old retry.
Existing completion continuation has priority. STOP/REVOKE, unknown-action
blocking and independent guidance applicability remain enforced.

## Frozen diagnostic comparison

One scenario per arm, run once. Input was an exact copy of the checkout's
eight-line requirements.txt. The task required a source-ordered dependency JSON
index with exact pinned versions, original line numbers and the input byte digest.
The safe prefix was explicitly scripted: one initial Mind Directive and three
identical actual isolated Python reads. These were four injected responses per
arm and zero real provider requests, not naturally generated model repetition.
No developer stall message or answer was supplied during live continuation.

Same task, input, prefix, baseline context mode and allocated total budget;
distinct isolated equivalent Run/Mind identities. Each arm had eight NEW requests
including every role and correction. Global ceilings: 24 calls, 393,216 reserved
output tokens and 5,760,000 request bytes. Official deepseek-v4-pro: Mind thinking
enabled/low, Execution thinking disabled/temperature 0; existing Builder policy.
There was no quota extension or resampling. Raw registration, requests, failures,
prefix provenance, code freeze and posthoc checks remain in private test storage.

| Result | A: off | B: Execution | C: plus Mind |
| --- | ---: | ---: | ---: |
| New calls: Execution / Mind / Builder | 4 / 2 / 2 | 4 / 2 / 2 | 1 / 5 / 2 |
| Further exact prefix repetitions | 0 | 0 | 0 |
| Objective JSON acceptance; input unchanged | yes | yes | yes |
| Runtime completed inside allowance | no | no | no |
| New Directive received / result reviewed | 0 / 0 | 0 / 0 | 1 / 1 |
| Reported output tokens | 2,314 | 2,709 | 8,590 |
| Actual request bytes | 127,774 | 127,323 | 185,518 |

A and B independently wrote and checked the index, proposed completion, and
entered result review. Mind read the product and requested an isolated SHA-256
calculation; the allowance ended after Builder's report, before final cognition.
C received the repetition fact in its first live Mind request, requested the
same type of calculation, then submitted a Directive. One missing submission
type required the existing bounded protocol correction. Execution received the
original Directive, wrote the index, and reported it. The same Mind read the
product and accepted NoChange, revising the prior missing-output belief. There
was no allocation left for Execution's runtime completion. All three products
passed the independent field/order/hash check; none is reported as a completed Run.

**A did not reproduce continued exact repetition**, so A-B gives no evidence of
benefit. Additionally, actual wire audit found frozen C's new Directive changed
the repetition scope and erased the fact before Execution's request. C received
advice but not B's same raw observation. B-C therefore fails the planned
same-evidence comparison. C's one action and reviewed product cannot establish
Mind superiority; its Directive also enumerated the entire index, exceeding the
intended concise direction in specificity. No new output gate was added.

All 24 real calls returned HTTP responses; one C cognitive submission used
protocol recovery. Totals: **319,488 reserved output tokens; 440,615 request bytes;
87,764 reported input_tokens; 13,613 output_tokens; 30,976 cache_read_input_tokens**.
These are raw provider usage fields, not a price estimate. Synthetic prefix
responses are disclosed separately. Calculation is not prospective watch coverage.

## Repairs and final evidence scope

Before live evaluation, regressions caught a reference-only Mind projection and
then the existing 1,000-character evidence bound. The fact now fits that bound;
full samples, source versions and action/result records stay external.

After the frozen comparison, final **execution-repetition-2** fixed three
confirmed problems without another real call:

- Explicit environment reads now save the new sampling boundary before Nervous
  can acknowledge it, preserving query-B / crash / return-A recovery.
- External samples are no longer copied back into event bodies. Full 10,000
  character Unicode/escaped results cannot overflow the event merely by triggering
  observation. Reads retain existing capacity responses and original source refs.
- Advice is no longer part of the repeated-observation condition. New advice
  cannot erase the fact before Execution sees it or rearm its notification.
  Action authority and stale-guidance checks still use their original binding.

The v1 comparison remains v1. These v2 fixes have deterministic evidence only;
they do not retroactively validate C or claim a v2 real-model success.

Final v2 validation: **1,023 passed, 30 skipped** in the maintained suite;
**14 passed** in the related isolated Docker checks, using scripted model replies.
Focused regressions cover actual wire delivery, same-Mind source retrieval,
NoChange-before-ack crash, new guidance plus restart, A-B-A, different scopes,
Task/Run separation, WAIT, unfinished/unknown actions, complete results versus
truncated prefixes, completed quiet restart, original completion continuation
and STOP/REVOKE. Early red tests and review reproductions were retained. One
full-suite attempt collected an earlier version of a new test fixture; its
assumption that an old Task review was current was corrected through a legal
same-Task review. No product state was edited to satisfy it.
Another run caught the new wire test still selecting the v1 version label;
the test now asserts the actual v2 contract, and the final complete suite passes.

Spec, causal-validity, authority, context and recovery review found no remaining
scoped implementation blocker after v2; behavior benefit remains unestablished.
No Chat/UI/Memory wiring, default context/pursuit change, commit or push occurred.

**Sole recommendation:** retain baseline/off; keep the new path opt-in for
diagnosing a future naturally occurring repeated segment. Do not expand this
completed slice or promote C from this result.
